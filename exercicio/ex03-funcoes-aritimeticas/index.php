<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Funções Aritimétocas</title>
    <link rel="stylesheet" href="style.css">
</head>
<body>
    <header>
        <h1>Funções Aritiméticas</h1>
    </header>
    <section>
        <?php 
            //abs() = Retorna o valor absoluto de um n
            // $r = abs(-500);
            // echo "A resposta é $r";
            
            // $r = base_convert(1011, 2, 10);
            // echo "A resposta é $r";

            //ceil() = pra cima; floor() pra baixo; round() = normal;

            //hypot() = Hipotenusa

            // $r = intdiv(5, 2);
            // echo "A resposta é $r";
            
            // $r = min(5, 2);
            // echo "A resposta é $r";
            
            // $r = max(5, 2, 4, 1, 9, 10, 2);
            // echo "A resposta é $r";
            
            // $r = pi();
            // echo "A resposta é $r";
            // $r = M_PI; // retorna PI
            // echo "A resposta é $r";

            // $r = pow(5, 2);
            // echo $r;

            // sin(), cos(), tan()

            // $r = sqrt(81);
            // echo $r;

            // $r = 81 ** (1/2);
            // echo $r;

        ?>
    </section>
</body>
</html>